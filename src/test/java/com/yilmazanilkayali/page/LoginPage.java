package com.yilmazanilkayali.page;

import com.yilmazanilkayali.credentials.WebPageCredentials;
import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.Cookie;
import org.openqa.selenium.WebDriver;

//public class LoginPage extends WebPage {

   /* private By emailBox = By.id("LoginEmail");
    private By passwordBox = By.id("Password");
    private By loginButton = By.id("loginLink");*/

    /*public LoginPage(WebDriver webDriver) {
        super(webDriver);*/
    //}

    /*private void enterEmail() {
        getElement(emailBox).sendKeys(WebPageCredentials.USER_EMAIL);

    }

    private void enterPassword() {
        getElement(passwordBox).sendKeys(WebPageCredentials.PASSWORD);
    }*/

    /*public void login() {
        enterEmail();
        enterPassword();
        getElement(loginButton).click();
        waitForPageLoad();
        checkLoginAttempt();
    }

    private void checkLoginAttempt() {
        Cookie cookieNamed = getDriver().manage().getCookieNamed("CV_UserId");
        if (cookieNamed == null) {
            Assert.fail();
        }*/
  //  }*/
//}*/

